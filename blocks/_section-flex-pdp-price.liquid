{% render 'section.flex-pdp.price',
  product: closest.product,
  product_save_amount: false
%}

{% schema %}
{
  "name": "t:flex_pdp.blocks.price.name",
  "presets": [
    {
      "name": "t:flex_pdp.blocks.price.name"
    }
  ]
}
{% endschema %}